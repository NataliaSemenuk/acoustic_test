export enum ThemeNames {
    DARK = 'dark',
    LIGHT = 'light'
};

export enum ElementTypes {
    TEXT = 'text',
    FORMATTED_TEXT = 'formattedtext',
    DATE_TIME = 'datetime',
    GROUP = 'group',
    IMAGE = 'image',
};
